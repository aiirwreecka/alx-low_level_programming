#include <stdio.h>

/**
 * main - Prints number from 0 to 9
 *
 * Return: Always (Success/correct)
 */

int main(void)
{
	int d;

	for (d = 0; n <= 9; d++)
	{
	putchar(d);
	}

	putchar('\n');

	return (0);
}
