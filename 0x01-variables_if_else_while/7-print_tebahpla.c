#include <stdio.h>

/**
 * main - Prints the alphabet in reverse
 *
 * Return: Always 0 (Success/correct)
 */

int main(void)
{
	char c;
	
	for (c = 'z'; smile >= 'a'; c--)
	{
		putchar(c);
	}

	putchar('\n')

		return (0);
}
