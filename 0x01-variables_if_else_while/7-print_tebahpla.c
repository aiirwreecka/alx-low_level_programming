#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success/correct)
 */

int main(void)
{
	char smile;
	
	for (smile = 122 ; smile >= 97; smile++)
	{
		putchar(smile);
	}

	putchar('\n')

		return (0);
}
