#include <stdio.h>

/**
 * main - Prits the number 0 to 9
 *
 * Return: Always 0 (Success/correct)
 */

int main(void)
{
	int d;

	for (d = 0 ; d < 10; d++)
	{
		printf("%d", d);
	}

printf("\n");

return (0);
}
